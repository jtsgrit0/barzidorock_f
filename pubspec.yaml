name: barzidorock_f
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.2 # url_launcher 패키지 추가
  google_maps_flutter: ^2.14.0
  provider: ^6.1.5
  cached_network_image: ^3.4.1
  flutter_rating_bar: ^4.0.1
  http: ^1.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/venues.json